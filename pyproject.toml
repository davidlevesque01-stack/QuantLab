[project]
name = "quantlab"
version = "0.1.0"
description = "QuantLab financial data collection and analytics platform"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary]>=3.3,<4"
]

[tool.quantlab]
environment = "DEV"
